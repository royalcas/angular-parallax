import { ParallaxJsDirective } from './parallax-js.directive';

describe('ParallaxJsDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxJsDirective();
    expect(directive).toBeTruthy();
  });
});
